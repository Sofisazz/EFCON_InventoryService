server:
  port: 8086

spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5434/inventory_service
    username: postgres
    password: 1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: classpath:db/changelog/master.yaml
    enabled: true
  http:
    client:
      connect-timeout: 60s
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      retry:
        enabled: true
        max-attempts: 3
        backoff:
          initial-interval: 1000
          multiplier: 2.0
          max-interval: 10000
    consumer:
      group-id: my-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
    properties:
      spring.json.trusted.packages: "*"
      spring.json.type.mapping: "com.example.menuservice.dto.ConsumeProductDto:com.example.inventoryservice.dto.ConsumeProductDto"

openfoodfacts:
    url: https://world.openfoodfacts.org/api/v0/product/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
  instance:
    prefer-ip-address: true

resilience4j.circuitbreaker:
  configs:
    default:
      registerHealthIndicator: true
      slidingWindowSize: 10
      minimumNumberOfCalls: 5
      permittedNumberOfCallsInHalfOpenState: 3
      automaticTransitionFromOpenToHalfOpenEnabled: true
      failureRateThreshold: 50
      waitDurationInOpenState: 10000
      eventConsumerBufferSize: 10
      ignoreExceptions:
        - com.example.inventoryservice.exceptions.MissingException
        - feign.FeignException$BadRequest
  instances:
    userService:
      baseConfig: default
      waitDurationInOpenState: 15s
      failureRateThreshold: 60